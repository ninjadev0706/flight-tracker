import React from 'react';
import { Nav } from './styles';

const Navbar = () => {
    return (
        <Nav>
            <nav>
                <span>Flight</span> Tracker
            </nav>
        </Nav>
    );
}

export default Navbar;
